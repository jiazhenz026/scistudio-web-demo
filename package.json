{
  "name": "scistudio-web-worker",
  "private": true,
  "description": "Cloudflare Worker + Containers front door for the SciStudio WebMCP demo",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.129.0"
  }
}
